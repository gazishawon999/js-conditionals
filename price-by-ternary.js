//If the price is greater than 1000, print "Expensive", otherwise "Affordable" — write this using a ternary operator.

let price = 999;
let result = price > 1000  ? "Expensive" : "Affortable";
console.log(result);